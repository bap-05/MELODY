<?xml version="1.0" encoding="utf-8"?>
<!-- Layout này định nghĩa giao diện cho MỘT hàng trong danh sách kết quả tìm kiếm -->
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="12dp"
    android:background="?attr/selectableItemBackground"> <!-- Thêm hiệu ứng gợn sóng khi nhấn -->

    <!-- Hình ảnh đại diện của bài hát (album art) -->
    <ImageView
        android:id="@+id/song_image"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_alignParentStart="true"
        android:layout_centerVertical="true"
        android:scaleType="centerCrop"
        tools:src="@drawable/nangam" /> <!-- Ảnh placeholder để xem trước trong Android Studio -->

    <!-- Cụm chứa Tên bài hát và Tên nghệ sĩ -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_toEndOf="@id/song_image"
        android:layout_centerVertical="true"
        android:layout_marginStart="16dp"
        android:orientation="vertical">

        <!-- Tên bài hát -->
        <TextView
            android:id="@+id/song_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@android:color/white"
            android:textSize="16sp"
            android:textStyle="bold"
            tools:text="Nắng Ấm Xa Dần" /> <!-- Dữ liệu giả để xem trước -->

        <!-- Tên nghệ sĩ -->
        <TextView
            android:id="@+id/song_artist"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="#A0FFFFFF"
            android:textSize="14sp"
            tools:text="Sơn Tùng M-TP" /> <!-- Dữ liệu giả để xem trước -->

    </LinearLayout>

</RelativeLayout>

